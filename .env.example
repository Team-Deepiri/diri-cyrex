# ============================================================================
# Cyrex Service - Environment Configuration
# ============================================================================
# For development: copy to .env and use as-is (defaults are fine)
# For production: generate all passwords with: openssl rand -base64 32
# ============================================================================

# --- API ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
CORS_ORIGIN=http://localhost:5173
NODE_BACKEND_URL=http://localhost:5000
CYREX_API_KEY=change-me

# --- Environment ---
# Set to "production" to enforce password validation
# NODE_ENV=development
# ENVIRONMENT=development

# --- Logging ---
LOG_LEVEL=INFO

# --- Cache dirs (optional; defaults: project root when not in Docker) ---
# ADAPTER_CACHE_DIR=./adapters
# MODEL_CACHE_DIR=./models/cache

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=                       # REQUIRED in production: openssl rand -base64 32

# --- PostgreSQL ---
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=deepiri
POSTGRES_USER=deepiri
POSTGRES_PASSWORD=deepiripassword     # Change in production: openssl rand -base64 32

# --- JWT ---
JWT_SECRET=default-secret-change-in-production  # Change in production: openssl rand -base64 48

# --- Local LLM ---
LOCAL_LLM_BACKEND=ollama
LOCAL_LLM_MODEL=llama3:8b
OLLAMA_BASE_URL=http://localhost:11434
